---
/**
 * Sidebar.astro - minimal sidebar navigation
 * Only "All Notes" for now.
 */
---
<aside class="sidebar">
  <nav>
    <a class="sidebar-link active" href="#">
      <svg width="20" height="20" fill="none" viewBox="0 0 20 20"><rect width="16" height="16" x="2" y="2" fill="#FFC107" rx="4"/><path d="M7 9h6v2H7z" fill="#1976D2"/></svg>
      <span>All Notes</span>
    </a>
  </nav>
</aside>

<style>
.sidebar {
  width: 230px;
  background: #f7f9fc;
  border-right: 1px solid #e5e7eb;
  padding: 2rem 0.5rem;
  min-width: 150px;
  min-height: 0;
  display: flex;
  flex-direction: column;
  gap: 2rem;
}

.sidebar nav {
  display: flex;
  flex-direction: column;
  gap: 0.6rem;
  margin-top: 0.4rem;
}
.sidebar-link {
  display: flex;
  align-items: center;
  gap: 0.55em;
  color: #424242;
  font-weight: 500;
  text-decoration: none;
  letter-spacing: 0.01em;
  border-radius: 4px;
  padding: 0.5em 1em;
  transition: background .15s, color .15s;
}
.sidebar-link svg {
  flex-shrink: 0;
}
.sidebar-link.active, .sidebar-link:hover {
  background: #e3f1fd;
  color: #1976D2;
}

@media (max-width: 768px) {
  .sidebar {
    width: 100%;
    min-width: 0;
    border-right: none;
    border-bottom: 1px solid #e5e7eb;
    flex-direction: row;
    padding: 0.5rem;
    overflow-x: auto;
    gap: 0.5rem;
  }
  .sidebar nav {
    flex-direction: row;
    width: 100%;
    gap: 0.5rem;
    margin-top: 0;
  }
  .sidebar-link {
    padding: 0.5em 0.95em;
    font-size: 1em;
  }
}
</style>
